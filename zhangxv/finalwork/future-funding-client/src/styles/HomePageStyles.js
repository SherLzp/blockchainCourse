import blue from '@material-ui/core/colors/blue';

const styles = theme => ({
  projectsGrid: {
    flexGrow: 1
  },
  addBtn: {
    margin: 0,
    top: 'auto',
    right: 20,
    bottom: 20,
    left: 'auto',
    position: 'fixed',
    backgroundColor: blue[600]
  }
})

export default styles;